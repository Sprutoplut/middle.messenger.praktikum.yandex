const PATH = {
  login: '/',
  register: '/sign-up',
  profile: '/settings',
  profileChangeData: '/settings-data',
  profileChangePassword: '/settings-password',
  chat: '/messenger',
} as const;

export default PATH;
